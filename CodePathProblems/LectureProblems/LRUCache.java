public class LRUCache {
    // LRU Cache class      put (1, a)
    // - capacity           put (2, b)  
    // - get (key)          put (3, c)
    // - put (key, value)   put (4, d)
    HashMap<Integer, Character> map = new HashMap<>();

    private static reachingCapacity (){

    }

    private int getKey(){
        return -1;
    }

    private int putKeyAndValue(){
        return -1;
    }
    Stack<Pair<Integer, Integer>> stack = new Stack<>();
    
}
